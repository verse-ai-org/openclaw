# pnpm build-script allowlist lives in package.json -> pnpm.onlyBuiltDependencies.

# hoist all dependencies to reduce nested node_modules depth
# This fixes Windows MAX_PATH (260 char) issues with NSIS installer
node-linker=hoisted
shamefully-hoist=true
